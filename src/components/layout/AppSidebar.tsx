// Skeleton — navegación completa (ítems por rol, ruta activa, contadores de
// tareas vencidas/notificaciones) se implementa en Fase 2.
export function AppSidebar() {
  return (
    <aside className="hidden w-64 shrink-0 border-r border-border bg-card md:block">
      <div className="p-4 text-sm text-muted-foreground">Sidebar — Fase 2</div>
    </aside>
  );
}
